{
  "name": "wealthtrack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev -- --host 0.0.0.0",
    "build": "npm --prefix client run build",
    "install:all": "npm --prefix client install --no-audit --no-fund"
  }
}